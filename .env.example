# Copy to .env and fill with your Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
# Must be a WhatsApp-enabled Twilio number, prefixed with whatsapp:
TWILIO_WHATSAPP_FROM=whatsapp:+17343367101

# =============================================================================
# RAILWAY DEPLOYMENT - PERSISTENT STORAGE (REQUIRED)
# =============================================================================
# Railway containers have ephemeral filesystems. You MUST mount a volume at /data
# and set these environment variables for pairing state to persist.
#
# In Railway dashboard:
#   1. Go to Settings → Volumes
#   2. Add New Volume
#   3. Mount Path: /data
#   4. Size: 1GB minimum
#
# Without this volume:
#   - Device pairing approvals will be lost on container restart
#   - Session data will be ephemeral
#   - Gateway will require re-pairing after each deployment
# =============================================================================
CLAWDBOT_STATE_DIR=/data/.clawdbot
CLAWDBOT_CONFIG_DIR=/data/.clawdbot

# Gateway authentication token (generate with: openssl rand -hex 32)
CLAWDBOT_GATEWAY_TOKEN=your_secure_gateway_token_here

# Remote pairing admin secret for approving devices without CLI (generate with: openssl rand -hex 32)
CLAWDBOT_PAIRING_ADMIN_SECRET=your_secure_admin_secret_here

# =============================================================================
# META (FACEBOOK / INSTAGRAM) PUBLISHING (OPTIONAL)
# =============================================================================
# Bootstrap token (used to resolve a Page token via GET /me/accounts).
META_ACCESS_TOKEN=your_meta_user_or_system_user_token_here
# Optional override: if set, OpenClaw uses this Page access token directly for Page/IG publishing.
META_PAGE_ACCESS_TOKEN=your_meta_page_access_token_here
# Facebook Page ID to publish to (and to resolve the linked Instagram business account).
META_PAGE_ID=1234567890
# Optional: used by Telegram /meta_debug diagnostics.
META_APP_ID=1234567890
META_APP_SECRET=your_meta_app_secret_here
