{
  "permissions": {
    "allow": [
      "Bash(xargs grep -l \"drop.*bot\\\\|allowBots\")",
      "Bash(npx --filter etsy-auto-post vitest run --reporter=verbose)",
      "Bash(npx vitest run --reporter=verbose)",
      "Bash(npx tsc --noEmit)"
    ]
  }
}
