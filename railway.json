{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install --frozen-lockfile && pnpm --dir apps/etsy-auto-post build",
    "env": {
      "NODE_VERSION": "22"
    }
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "node apps/etsy-auto-post/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "healthcheckInterval": 15
  },
  "volumes": [
    {
      "mountPath": "/data",
      "sizeGB": 1
    }
  ]
}
